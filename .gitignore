# ── IDE ──
*.iml
.idea/
.vscode/
.DS_Store

# ── Gradle / Android build output ──
.gradle/
**/build/
local.properties
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.ap_
*.dex
.kotlin/

# ── Logs / profiling ──
*.log
*.hprof

# ── Local agent/tool state (never commit) ──
.freebuff/
.agents/
.claude/
.zcode/
# ── Local tooling output ──
graphify-out/

# ── Runtime models (downloaded on demand, not vendored) ──
app/src/debug/assets/models-dev/
keystore.properties
*.jks

# ── Local QA harness / scratch ──
build/qa/
tools/strip_comments.py
